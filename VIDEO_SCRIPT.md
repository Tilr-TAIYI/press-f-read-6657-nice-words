# CS2 烂梗助手介绍视频文稿

## 建议时长：60-75 秒

### 0-6 秒：开场

**画面**：桌面上双击 `Sb6657Cs2Assistant.exe`，窗口直接打开。

**旁白**：
“玩 CS2 聊天时，想快速换点有趣的内容？这是 CS2 烂梗助手，下载后双击就能用，不需要安装运行环境。”

### 6-20 秒：一键开始

**画面**：展示标签列表、当前内容和“一键启动”按钮，Steam 与 CS2 状态自动变为已检测。

**旁白**：
“程序会自动查找 Steam 和 CS2，标签、随机内容和下一条缓存都会自动准备好。点击一键启动，助手按设定间隔把下一条内容复制到剪贴板。”

### 20-34 秒：游戏内使用

**画面**：切到 CS2 聊天框，按 `Ctrl+V` 粘贴并由玩家手动发送；回到助手查看发送历史和统计。

**旁白**：
“回到游戏聊天框，按你想要的按键就可以直接一键发送。当前内容、发送历史、成功和失败次数，都能在面板里清楚查看。”

### 34-50 秒：可选按键绑定

**画面**：退出 CS2，选择发送键，点击“应用按键绑定”，再启动 CS2；演示按键触发下一条。

**旁白**：
“如果你想用实体按键切换内容，完全退出 CS2 后应用一次按键绑定即可。这是可选功能，默认不会监听全局键盘；只有你主动应用绑定后才会启用。”

### 50-64 秒：安全说明

**画面**：安全说明、备份目录和 CFG 所有权标记的特写。

**旁白**：
“它不注入游戏、不读写进程内存，也不模拟键盘输入。网络请求只走固定 HTTPS 接口，配置写入范围受限，原始按键会先备份，陌生 CFG 不会被覆盖。”

### 64-75 秒：兼容性与收尾

**画面**：Steam 库自动发现、版本更新后的启动画面，最后显示 Release 下载位置。

**旁白**：
“CS2 普通更新后，程序会重新发现 Steam 库和用户配置，继续使用公开的 CFG 接口。下载 Release，双击 EXE，马上开始。”

**屏幕字幕建议**：
“自包含单文件｜无需配置环境｜剪贴板优先｜按键绑定可选｜配置自动备份”
